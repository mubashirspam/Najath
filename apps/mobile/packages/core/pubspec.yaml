name: najath_core
description: "Config, constants, errors, secure storage, permissions and responsive primitives."
version: 0.0.1

resolution: workspace
publish_to: 'none'

environment:
  sdk: ^3.12.2
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.0
  flutter_secure_storage: ^10.3.1
  dio: ^5.11.0
  collection: ^1.19.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

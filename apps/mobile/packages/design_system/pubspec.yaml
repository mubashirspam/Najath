name: najath_design_system
description: "Najath theme, typography and the shared widgets every feature reuses."
version: 0.0.1

resolution: workspace
publish_to: 'none'

environment:
  sdk: ^3.12.2
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.0
  google_fonts: ^8.2.1
  shimmer: ^3.0.0
  najath_core:
    path: ../core
  najath_network:
    path: ../network
  najath_auth:
    path: ../auth
  najath_local_db:
    path: ../local_db
  najath_sync:
    path: ../sync

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

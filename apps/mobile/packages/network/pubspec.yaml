name: najath_network
description: "DioClient, ApiResponse, interceptors and the connectivity providers."
version: 0.0.1

resolution: workspace
publish_to: 'none'

environment:
  sdk: ^3.12.2
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.0
  dio: ^5.11.0
  connectivity_plus: ^7.3.1
  najath_core:
    path: ../core

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

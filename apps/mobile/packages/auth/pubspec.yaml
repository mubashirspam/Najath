name: najath_auth
description: "Staff password and guardian OTP sign-in, session bootstrap, and the role/screen access policy."
version: 0.0.1

resolution: workspace
publish_to: 'none'

environment:
  sdk: ^3.12.2
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.0.0
  najath_core:
    path: ../core
  najath_network:
    path: ../network
  najath_local_db:
    path: ../local_db

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
